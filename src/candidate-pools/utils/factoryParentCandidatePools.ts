import { CandidatePool } from "@prisma/client"

const factoryCandidatePools = [
  { name: "Notice Period Mismatch" } as CandidatePool,
  { name: "Salary Expectation Mismatch" } as CandidatePool,
  { name: "Accepted Other Offer" } as CandidatePool,
]

export default factoryCandidatePools
